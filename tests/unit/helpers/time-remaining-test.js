import { timeRemaining } from 'admin-frontend/helpers/time-remaining';
import { module, test } from 'qunit';

module('Unit | Helper | time remaining');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeRemaining([42]);
  assert.ok(result);
});
